# Server-side only — do not use NEXT_PUBLIC_ for these (never expose to the client).
# Copy to .env.local and fill in your keys.

# Google AI (Gemini Flash) — get key at https://aistudio.google.com/apikey
# When set, chat uses Gemini 1.5 Flash for Sarah's replies (rules still drive cart/order state).
GOOGLE_GENERATIVE_AI_API_KEY=

# ElevenLabs — get key at https://elevenlabs.io
ELEVENLABS_API_KEY=
